# --- DATABASE (POSTGRESQL) ---
POSTGRES_USER=my_admin
POSTGRES_PASSWORD=super_secret_password
POSTGRES_DB=web_base

# --- BACKEND ---
SECRET_KEY=chuoi_ky_tu_cho_jwt
API_V1_STR=/api/v1

# --- FRONTEND ---
# Dùng path tương đối cho API; target proxy chỉ cần cho Vite dev.
VITE_API_URL=/api/v1
